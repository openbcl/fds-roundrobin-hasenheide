# Office source files – only distribute final PDFs
*.docx
# …except the fillable questionnaire, which is part of the data package
!docs/questionnaire_fillable.docx
*.xlsx
*.pptx
# Literature PDFs in docs/ – exclude CfP files
docs/*.pdf
!docs/Call_for_Participation_*.pdf
# Participant submissions and operational data
participants/*
# Internal working notes (local only)
backlog.md
# Participant submissions (local only, not for publication)
results/
# Evaluation pipeline (local only; scripts released at Stage 2)
analysis/
# Kick-off presentation materials (local only, not for publication)
kickoff/